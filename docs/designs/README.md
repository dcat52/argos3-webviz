# Design Documents

Detailed implementation specs for code-heavy proposals. Each file is named
`PN-xxx-<name>.md` matching its parent proposal.

These are working documents that evolve during implementation. The proposal
captures intent; the design doc captures specifics (function signatures,
data structures, config files, etc.).
